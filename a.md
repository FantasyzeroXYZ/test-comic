* 修改成两种模式，一种是左右切换，一种是上下滑动（适配条漫）
  * 左右模式是点击左侧边缘或者右侧边缘可以触发换页。
  * 无论是上下模式还是左右模式，你最起码需要在页面里自适应的完整的显示一页出来
* 能够自适应不同大小显示，可以放大缩小，换页后可以返回原来的自适应大小
* 适配移动端显示和操作
* 可以通过按键设置是否高亮显示对应坐标
* 当图片放大时点击词典弹出显示要求词典框大小保持不变，不随页面其他内容一并放大缩小
* 修改后返回完整代码，html返回html，js返回js，css返回css。不要混搭

“响应式容器 + 绝对定位 overlay + viewBox 自适应 + JavaScript 动态设置宽度”

自动让图片根据屏幕大小缩放	Responsive Image（响应式图片）
自动让容器跟随屏幕变化	Responsive Container（响应式容器）
容器中叠加局部元素（OCR 坐标）	Absolute Positioning（绝对定位）
SVG 覆盖图片并同步缩放	SVG viewBox 自适应机制
切换固定/浮动模式	JS 动态设置元素宽度                                      把漫画放入这种里面，可以自适应显示

# 添加
* [ ] 局域网访问anki
  * [ ] ipad？
    * 目前是无法发送请求
  * [ ] 安卓？
* [ ] 临时存储漫画文件，保存进度

# 修复
* [ ] 防止放大缩小时误触打开词典
